name: Release

on:
  push:
    branches: 
      - main
      - dev

permissions:
  id-token: write
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/ci_workflow_cdk.yml
    with:
      run-version-check: false
  cd:
    needs: ci
    uses: ./.github/workflows/cd_workflow_cdk.yml
