name: CI (PR)

on:
  pull_request:
    branches: 
      - main
      - dev

permissions:
  contents: read
  id-token: write
  pull-requests: write

jobs:
  ci:
    uses: ./.github/workflows/ci_workflow_cdk.yml
    with:
      run-version-check: true
    secrets: inherit 
